package main

import (
	"log"

	"github.com/gocopper/copper"
)

func main() {
	app, err := copper.InitApp()
	if err != nil {
		log.Fatal(err)
	}

	server, err := InitServer(app)
	if err != nil {
		log.Fatal(err)
	}

	app.Start(server)
}
